{
  "name": "presentation",
  "version": "0.1.0",
  "scripts": {
    "build:pdf": "marp --html src/slides.md -o dist/slides.pdf",
    "build:html": "marp --html src/slides.md -o dist/slides.html",
    "build": "yarn build:pdf && yarn build:html",
    "dev": "marp --html --server ."
  },
  "dependencies": {
    "@marp-team/marp-cli": "git+https://github.com/GuillaumeDesforges/marp-cli#converter-async-render",
    "@mermaid-js/mermaid-cli": "^10.3.0",
    "jsdom": "^22.1.0"
  }
}